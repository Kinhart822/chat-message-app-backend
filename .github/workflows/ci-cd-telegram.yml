name: CI-CD with Telegram Notifications
run-name: ${{ github.actor }} is triggering workflow on ${{ github.ref_name }} 🚀

on:
  pull_request:
    types: [opened, reopened, ready_for_review, synchronize, closed]
  push:
    branches:
      - 'testing'
      - 'main'

jobs:
  main-workflow:
    name: Main Workflow Execution
    # runs-on: [self-hosted, tekchat-main-be]
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: 🛰️ Checkout repository
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.14.0'
          cache: 'yarn'

      - name: 📦 Install Dependencies
        run: yarn install --frozen-lockfile

      - name: 🧪 CI Checks (Lint, Test, Build)
        run: |
          npm run lint
          npm run test
          npm run build

      - name: 🔍 Get Latest Commit Message
        id: commit_info
        if: always()
        shell: bash
        run: |
          commit_msg=$(git log -1 --pretty=format:%s)
          echo "commit_msg=$commit_msg" >> $GITHUB_OUTPUT

      - name: 📊 Determine Event Status
        id: event_status
        if: always()
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            if [[ "${{ github.event.pull_request.merged }}" == "true" ]]; then
              echo "title=PR MERGED 🏆" >> $GITHUB_OUTPUT
            elif [[ "${{ github.event.action }}" == "closed" ]]; then
              echo "title=PR CANCELED ❌" >> $GITHUB_OUTPUT
            else
              echo "title=NEW/UPDATED PULL REQUEST 🔔" >> $GITHUB_OUTPUT
            fi
            echo "branch=${{ github.head_ref }}" >> $GITHUB_OUTPUT
          else
            echo "title=PUSH TO BRANCH 🚀" >> $GITHUB_OUTPUT
            echo "branch=${{ github.ref_name }}" >> $GITHUB_OUTPUT
          fi

      - name: 📢 Notify Telegram
        if: always()
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_CHAT_ID }}
          token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          format: markdown
          message: |
            ✨ **${{ steps.event_status.outputs.title }}**

            ───────────────────────
            📂 **Project:** `Tekchat Main BE`
            👤 **Actor:** `${{ github.actor }}`
            🌿 **Branch:** `${{ steps.event_status.outputs.branch }}`

            📝 **Message**
            ➜ `${{ steps.commit_info.outputs.commit_msg }}`

            ───────────────────────
            🚀 **Build Status:** ${{ job.status == 'success' && '✅ **SUCCESS**' || '❌ **FAILED**' }}
            ───────────────────────

            🔗 [View Details on GitHub](${{ github.event.pull_request.html_url || format('{0}/{1}/commit/{2}', github.server_url, github.repository, github.sha) }})
